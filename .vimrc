"I created a file using the command vim and naming it .vimrc. Then I edited
"the file to have the configurations below. Then I used to the i key to
"edit. Then I used esc, :, and wq to save and leave the file. After I used the cat command to print everything inside the .vimrc file."
syntax on 
set ruler 
set autoindent
set number
set ignorecase
color elflord

